MONGO_URI=mongodb://127.0.0.1:27017/user-role-auth
JWT_SECRET=your-secret-key
SERVER_PORT=3000



# FFmpeg Windows path (update this)
FFMPEG_PATH=C:\\ffmpeg\\bin\\ffmpeg.exe  
 # Optional: Only needed if FFmpeg is not in system PATH

# File Upload Configuration (optional - defaults are set in code)
 MAX_FILE_SIZE=104857600
MAX_FILES_COUNT=10


MAX_CONCURRENT_PROCESSES=3      # Limit concurrent FFmpeg operations
TEMP_DIR=public/uploads/temp    # Directory for temporary audio processing files

# Upload Directory (optional - defaults to ./uploads/audio)
# UPLOAD_DIR=./uploads/audio